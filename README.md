# claude-fp-token-tracker
This minimal tool measures Claude's free-plan context window fill (tokens used / 200k), not the free-plan message quota. The token tracking is precise, but the quota is sadly opaque and there is no way to truly measure it for the free-plan as of now.
